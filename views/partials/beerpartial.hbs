<div class="beer-container">
    
    <div class="beer-image">
      {{#if image_url}}
      <img src="{{this.image_url}}" alt="image"/>
      {{else}}
      <h2>This image doesn't exists</h2>
      {{/if}}
    </div>    
    <a href=""><p>{{this.name}}</p></a>
    <p>{{this.tagline}}</p>
    <p>{{this.description}}</p>
  
</div> 
